name: Update model pricing

on:
  schedule:
    - cron: "17 18 * * 0"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run update:model-pricing
      - run: >-
          npm run test --
          scripts/update-model-pricing.test.mjs
          packages/metadata/src/modelPricing.test.ts
          packages/metadata/src/usage.test.ts
      - run: npm run typecheck
      - run: npm run check:model-pricing
      - uses: peter-evans/create-pull-request@v7
        with:
          branch: codex/update-model-pricing
          commit-message: Update model pricing metadata
          title: Update model pricing metadata
          body: |
            Automated weekly refresh from official model pricing pages.

            Sources:
            - https://docs.anthropic.com/en/docs/about-claude/pricing
            - https://developers.openai.com/api/docs/pricing?latest-pricing=standard#text-tokens
            - https://developers.openai.com/api/docs/pricing?latest-pricing=fast#text-tokens
            - https://ai.google.dev/gemini-api/docs/pricing
          labels: automated
